# Backend environment example for access control and Cognito

# AWS region where your Cognito User Pool is
AWS_REGION=eu-west-1
# DynamoDB table and index (if you changed names)
DYNAMODB_TABLE_NAME=gcse_app
DYNAMODB_GSI1=GSI1

# OpenAI (optional for AI help)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Cognito verification config (required)
COGNITO_USER_POOL_ID=eu-west-1_XXXXXXXXX
COGNITO_APP_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional access control (enable any of these)
# Only allow users with this Cognito group
# REQUIRED_GROUP=members
# Only allow these exact emails or Cognito usernames (comma-separated)
# ALLOWED_EMAILS=alice@example.com,bob@example.com
# Only allow emails from these domains (comma-separated; with or without @)
# ALLOWED_DOMAINS=school.edu,example.org
